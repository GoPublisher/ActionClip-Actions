{
  "name": "actionclip-actions",
  "version": "0.1.0-private",
  "private": true,
  "description": "Official marketplace action definitions for ActionClip.",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-actions.mjs",
    "check:generated": "node scripts/sync-actions.mjs --check",
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate && npm run check:generated"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}

